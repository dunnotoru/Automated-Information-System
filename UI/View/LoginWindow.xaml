<Window x:Class="UI.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.View"
        xmlns:vm="clr-namespace:UI.ViewModel"
        mc:Ignorable="d"
        Height="467" Width="665" ResizeMode="NoResize" Icon="/View/icon.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90*"/>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="79*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Вход в учётную запись" 
                   VerticalAlignment="Bottom" HorizontalAlignment="Center" >
        </TextBlock>

        <TextBox Text="{Binding Username}" Margin="100,20,100,0" Width="350" VerticalAlignment="Top"
                 Grid.Row="1"/>

        <TextBox Text="{Binding Password}" Margin="100,70,100,20" Width="350" VerticalAlignment="Bottom"
                  Grid.Row="1" />

        <Button Content="Войти" Command="{Binding LoginCommand}" HorizontalAlignment="Center" Margin="0 0 0 0" Grid.Row="2" VerticalAlignment="Center" Height="20" Width="40"/>
    </Grid>
</Window>
