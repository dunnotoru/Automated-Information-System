<UserControl x:Class="UI.View.RunSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="23*"/>
            <RowDefinition Height="22*"/>
        </Grid.RowDefinitions>
        <ListView ItemsSource="{Binding StationItems}" SelectedItem="{Binding DepartureStation}" Margin="10,36,640,28">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Название"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Address}" Header="Адрес"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView ItemsSource="{Binding StationItems}" SelectedItem="{Binding ArrivalStation}" Margin="189,36,461,28">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Название"/>
                    <GridViewColumn DisplayMemberBinding="{Binding Address}" Header="Адрес"/>
                </GridView>
            </ListView.View>
        </ListView>
        <ListView ItemsSource="{Binding RunItems}" Margin="10,44,244,26" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding Number}" Header="Рейс"/>
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox HorizontalAlignment="Left" Margin="40,216,0,0" TextWrapping="Wrap" Text="Поиск станции" VerticalAlignment="Top" Width="120" Grid.RowSpan="2"/>
        <Calendar HorizontalAlignment="Left" Margin="590,36,0,0" VerticalAlignment="Top"/>
        <Calendar HorizontalAlignment="Left" Margin="377,35,0,0" VerticalAlignment="Top"/>
        <Label Content="Рейсы" HorizontalAlignment="Left" Margin="10,13,0,0" VerticalAlignment="Top" Grid.Row="1"/>
        <Label Content="Отправление" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>
        <Button Content="Найти рейсы" Command="{Binding FindRunsCommand}" HorizontalAlignment="Left" Margin="642,71,0,0" VerticalAlignment="Top" Height="32" Width="127" Grid.Row="1"/>
        <Button Content="Продажа билета" Command="{Binding SellTicketCommand}" HorizontalAlignment="Left" Margin="642,119,0,0" VerticalAlignment="Top" Height="32" Width="127" Grid.Row="1"/>

        <TextBox HorizontalAlignment="Left" Margin="219,216,0,0" TextWrapping="Wrap" Text="Поиск станции" VerticalAlignment="Top" Width="120" Grid.RowSpan="2"/>
        <Label Content="Прибытие" HorizontalAlignment="Left" Margin="189,10,0,0" VerticalAlignment="Top"/>
        <Label Content="Дата отправления" HorizontalAlignment="Left" Margin="377,10,0,0" VerticalAlignment="Top"/>
        <Label Content="от" HorizontalAlignment="Left" Margin="534,9,0,0" VerticalAlignment="Top"/>
        <Label Content="до" HorizontalAlignment="Left" Margin="590,10,0,0" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
