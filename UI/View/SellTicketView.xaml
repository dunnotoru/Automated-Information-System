<UserControl x:Class="UI.View.SellTicketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <TextBox Height="30" Width="200"
                 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 70"/>
        
        <TextBox Height="30" Width="200"
                 HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 110"/>

        <Button Height="50" Width="200"
            HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 10 10"
            Content="Подтвердить"/>
    </Grid>
</UserControl>
